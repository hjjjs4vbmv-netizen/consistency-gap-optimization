# ECT 项目交接文档 — 2026-08-10(Role C)

---

## 1. 今日工作总览

今天围绕 ICLR 2027 战略,用**实测数据**把方案的生死判断钉死,并产出诊断论文框架。

| 目标 | 状态 | 交付物 |
|------|------|--------|
| 干净的 g=1.3 vs g=1.0 FID 对比(补上仓库缺失的实测) | ✅ | `analysis/g13_vs_g10_fid_result.md` |
| E5 残差结构分解 | ✅ | `analysis/e5_residual_structure_result.md` |
| E6 剂量响应(残差 vs gap, FID vs gap) | ✅ | `analysis/e6_dose_response_result.md` |
| E7 优化器状态依赖(直接法绕过审计门控) | ✅ | `analysis/e7_optimizer_state_result.md` |
| E11 稳健性(跨 seed + 支撑阈值) | ✅ | `analysis/e11_robustness_result.md` |
| E3 普适性(AdamW 复现梯度残差) | ✅ | `analysis/e3_universality_result.md` |
| 诊断论文框架 + LaTeX PDF | ✅ | `docs/ICLR2027_DIAGNOSIS_PAPER.{md,tex,pdf}` |
| 战略文档同步(实测诊断证据) | ✅ | `docs/ICLR2027_STRATEGY.tex`(编译通过) |

**核心结论(全部实测):** 非标量梯度残差存在、有结构、状态条件、普遍、**无害**——
带它的 g=1.3 臂在 FID/KID 上大幅胜出 g=1.0(−34%~−45%)。**"机制+方法"前提被证伪,
E2 因果干预被反指,论文必须收缩为诚实诊断。**

---

## 2. 分支与 git 状态(重要)

- **本地分支**: `role-c/g13-vs-g10-fid-0809`(11 个 commit,**未合并 main**)
- **服务器分支**: 同名 `role-c/g13-vs-g10-fid-0809`(已创建)
- **规范**: 服务器只 pull + 本地 commit;push 在本地做(见 HANDOFF_20260804.md §4.5)
- **未 push**: 本地分支尚未 push 到 GitHub(需本地 push,勿推 main)

**分支 commit 列表:**
```
8c8474e docs: integrate E3 universality
3ccf552 analysis(E3): universality (AdamW)
98ab03e docs: diagnosis paper LaTeX
18e5597 docs: integrate E7/E11
158cf00 analysis(E7): optimizer-state dependence
79a51e9 analysis(E11): robustness
a47a116 docs: honest-diagnosis paper framework
224afb6 docs+analysis: sync E5/E6
9a0193f analysis(E6): dose-response
e465216 analysis(E5): residual structure
6d8c235 docs: sync g=1.3-vs-1.0 FID
40ab790 analysis: clean g=1.3-vs-1.0 FID
```

---

## 3. 服务器使用情况(按 HANDOFF_20260804.md 规范)

- **账户**: ECT002(172.16.30.17, gpu0003)
- **连接**: `python C:\Users\ASUS\ssh_helper.py "<cmd>"`(已配置 ECT002)
- **GPU**: A100 80GB,当前空闲(0 残留进程)
- **运行过的实验**:
  - g=1.3-vs-1.0 FID 评估 → `/data/raw/ECT/ect_runs/g13_vs_g10_fid_0809/`
  - E7 直接法审计(arm_a 各 tick)→ 结果在 `/tmp/e7_all.log`(已清理)
  - E3 AdamW → `/tmp/e3_adamw.log`(已清理)
- **清理**: 已清理 /tmp 日志、失败的 E7 输出目录;无残留进程
- **注意**: 审计 `radam_stateful_update_audit.py` 的 main() 要求 checkpoint 为
  sigmoid,arm 状态是 global_sigmoid——E7 用直接调用 `run_stateful_pair` 绕过
  (见 `analysis/run_e7_direct.py`)

---

## 4. 诊断证据链(全部实测,已整合)

| 环节 | 实验 | 结论 |
|---|---|---|
| 存在 | moment-memory | 标量 null 被证伪(h 1.001 vs 0.837) |
| 有结构 | E5 | 深度趋势(ρ≈0.4)+ 剂量响应 |
| 无害 | E6 + 干净 FID | 与 FID 反相关 r=−0.99;g=1.3 赢 −34%~−45% |
| 状态条件 | E7 | R_grad 随 n_K 增长(0.031→0.091) |
| 稳健 | E11 | 跨 3 seed(1024k 5/6)+ 支撑阈值 |
| 普遍 | E3 | 梯度残差跨 optimizer 复现(AdamW 0.095 vs RAdam 0.091) |

**关键数字:**
- a_K*=0.761, R_grad=5.85%, R_opt=8.57%, H_K=R_opt 精确(1.7e-18)
- h_predicted=1.001 vs h_actual=0.837, Corr≈0
- g=1.3 vs g=1.0: FID-5k −34%/−36%, KID-5k −37%/−45%(NFE1/2)

---

## 5. 战略结论(诚实)

- **"机制+方法 40-50%"被放弃**。残差有害前提被实测证伪(g=1.3 赢 FID)。
- **E2 因果干预被反指**(Arm C 投影回 g=1.0 会向更差 FID 移动),降级为探索性消融。
- **论文定位:诚实诊断**(约 20-30%):非标量梯度内容在少步训练中系统性存在、有结构、
  普遍、但良性。
- 诊断论文框架 + LaTeX PDF 已产出:`docs/ICLR2027_DIAGNOSIS_PAPER.{md,tex,pdf}`。

---

## 6. 建议的下一步

1. **本地 push 分支**(勿推 main):`git push origin role-c/g13-vs-g10-fid-0809`
2. **完善诊断论文**:补 E3 进 LaTeX、写 Introduction/Related work 完整段落、加图
3. **可选补强**:E2 作为探索性消融(验证"残差修正方向与质量方向相反")、更多诊断细节
4. **清理**:`ect_runs/` 下的 bench_*.bin、zip 等辅助文件(见 HANDOFF_20260804.md §7)

---

## 文件索引
- 诊断论文:`docs/ICLR2027_DIAGNOSIS_PAPER.{md,tex,pdf}`
- 战略:`docs/ICLR2027_STRATEGY.tex`(编译 PDF)
- 审查:`docs/ICLR2027_PLAN_REVIEW.md`
- 实验结果:`analysis/g13_vs_g10_fid_result.md`、`e5/e6/e7/e11/e3_*_result.md`
- 记忆:`iclr2027-gfct-strategy`(已更新实测结论)
