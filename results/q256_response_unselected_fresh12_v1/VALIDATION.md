# F12 q256 结果复核

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Origin Mode: validate
- Origin Date: 2026-09-14
- Verification Status: VERIFIED — 仅限公开表的 CPU 统计复算和一致性核对
- Version Label: f12_final_validation_v1
- Overall Confidence: CAUTION — H 的区间上界靠近零、n=12，且存在一次提前查看的流程偏离

## 复算结论与范围

`reproduce.py` 从全部288块重建12个逐种子主终点，使用 SciPy 的单样本 t 实现复核配对差；普通区间、90%区间、TOST、Holm族及可用配对口径全部核对通过。336项数值比较的最大绝对差为 `2.842170943040401e-14`，来自评估 GPUh 求和顺序；容差明确为 `1e-12`。H 均值和双侧 p 与冻结输出数值相同，Holm 的差约为 `2.1e-17`。没有覆盖或重新运行原始最终分析入口。

| 项目 | 冻结值 | 复算结论 |
|---|---:|---|
| H mean log | -0.06705393349835055 | 相同 |
| H 双侧 p | 0.04202413829666083 | 相同 |
| H 95% ratio CI | [0.877026332644501, 0.9971145651359422] | 相同 |
| M/C/S Holm p | 0.052462803459901716 | 数值容差内一致，均未通过0.05 |
| H TOST p | 0.8876018136346008 | 数值容差内一致，未建立等价 |

本次复算环境：macOS、Python 3.13.9、NumPy 2.3.5、SciPy 1.16.3。原科学环境是 Python 3.11.13、NumPy 2.1.2、SciPy 1.16.1；因此不把所有浮点派生量称为跨环境逐位一致。逐字节复制的CSV、冻结文档和代码另用原始SHA256精确核对。

检查包括48路径、288块及身份唯一性；三块固定生成范围、FP32、E_512、评估器及checkpoint绑定；四臂完整病例n=12；全部预指定统计；NFE2/KID描述性均值；48路径时钟/LR审计表、618 skips、12组范数配对和96份切换快照记录；公开目录的完整SHA256清单。没有新增终点检验、逐种子排除或训练重复。

端点checkpoint哈希在每路径6块间一致。冻结worker按NFE分别导出E_512.pkl，所以snapshot序列化文件哈希只在同一NFE的3块内要求一致；公开表保留了两个导出哈希。不能把不同NFE的pickle字节差异直接判断为不同训练端点，也不能仅凭这里的公开表独立证明两个导出的张量逐位相同。

工程表由运行期遥测审计生成，本次检查的是这些表的数值和身份一致性；没有重新读取499.81 GB的模型/生成数组，也没有重新运行GPU训练、FID/KID提取或工程训练。完整原始数据复现不在本次 `VERIFIED` 的范围内。八份完整源端导出和两份先前退租归档证明保留于中央归档；公开JSON中的原件哈希是追溯依据，不能单凭哈希替代原件访问。

## 解释边界

H达到预设固定样本双侧0.05阈值，几何FID降幅6.49%，95%区间为[-12.30%, -0.29%]。区间容许很小的收益，精度有限；不预先套用C14的方差保证本轮检出能力。配对t推断依赖种子间独立及种子差的分布假设；n=12时不能仅凭检验通过认定这些假设已经充分验证。本次没有用事后正态性检验选择另一种主分析。

一次用户要求的提前查看确实发生，详见 [interim_disclosure.json](interim_disclosure.json)。当时有117个NFE1块、7个完整种子；已记录、未删除，没有据此变动样本、对比、预备队列或停止规则。最终p值是原固定样本计算，未作顺序检验校正。本轮不得写成“严格无期中查看的全程盲态确证”。

M/C/S的名义p均小于0.05，但三者Holm p均为0.052463；主文不得把名义区间不跨零解释为家族检验通过。S先在种子内构造，保留M/C协方差，其指数不是单个模型的FID改善比例。H未建立±log(1.03)等价，不代表已经证明所有实用效应；不同spacing各自是否显著不构成spacing交互检验。

A↓5前五次范数和降低只出现10/12，D↑5升高只出现11/12。开跑前修订第3条已将方向作为描述项；所有种子保留，不能按范数响应重新筛选。两次评估控制器预启动退出及后续资源迁移保留记录，不把最终零失败计数写成“运行期间从未发生技术中断”。

## 11类统计/方法谬误扫描

覆盖：11/11类均已考虑；“不适用/资料不足”表示未作相应的额外检验，不等于证明该风险不存在。

| 类型 | 评估 | 与本结果的关系 |
|---|---|---|
| Simpson悖论 | 范围受限 | 使用种子内配对；没有预指定硬件/亚组交互分析，未新增分层检验。 |
| 生态谬误 | 未见相应越界 | 单位是训练种子，generation block不作为独立训练样本；不推断到单张图片。 |
| Berkson选择偏差 | 未见响应筛选 | 固定401–412全部保留，未借用历史响应筛选队列。 |
| Collider偏差 | 完成性风险未实现 | 完整病例规则可能在有依赖响应的失败时造成选择；本轮12/12完整，没有这种删除。 |
| 基率忽略 | 不适用 | 不是筛查灵敏度、特异度或预测值研究。 |
| 回归均值 | 未按极端响应选种子 | 全新连续种子且有同种子AA对照，不将历史高响应者的再测当成本轮样本。 |
| 幸存者偏差 | 完成性完整 | 48路径和288块全部PASS，列出所有最终失败计数及历史预启动中断。 |
| 多处寻找显著性 | 需保留边界 | 只报告预指定H与M/C/S；Holm处理固定次要族，NFE2/KID不做检验。提前查看另外披露。 |
| 分析分叉 | CAUTION | 方案/脚本在开跑前冻结；有一次提前查看偏离，无响应驱动变更；不宣称完美盲态。 |
| 相关当因果 | 限定实验配置 | 有实际训练干预与匹配初始化；支持该配置下的对比，不证明唯一机制或跨配置普遍收益。 |
| 反向因果 | 未见相应设计问题 | 干预和LR窗口发生在终点评估前；终点未用于分配、排除或改变干预。 |

图已目视检查：12个种子点和一个整体配对t区间，未给generation block添加伪训练样本区间。公开包扫描未发现云机器连接地址、私有存储路径、GPU UUID或认证字段。原始记录继续保留。
